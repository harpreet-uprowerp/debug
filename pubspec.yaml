name: debug
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+10

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  carousel_slider: 2.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.15.7
  flutter_driver:
    sdk: flutter

flutter:
  uses-material-design: true